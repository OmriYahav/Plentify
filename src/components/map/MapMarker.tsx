import React from 'react';import {StyleSheet,Text,View} from 'react-native';import {Marker} from 'react-native-maps';import {CATEGORIES} from '@/constants/categories';import {LISTING_TYPES} from '@/constants/listingTypes';import {Listing} from '@/types/models';
export function PlentifyMarker({listing,onPress}:{listing:Listing;onPress:()=>void}){const c=CATEGORIES[listing.category],lt=LISTING_TYPES[listing.type];return <Marker coordinate={{latitude:listing.lat,longitude:listing.lng}} onPress={onPress} tracksViewChanges={false}><View style={[s.pin,{borderColor:lt.color,backgroundColor:c.color}]}><Text style={s.icon}>{listing.type==='REQUEST'?'?':c.emoji}</Text></View></Marker>}
const s=StyleSheet.create({pin:{width:40,height:40,borderRadius:20,alignItems:'center',justifyContent:'center',borderWidth:3,shadowColor:'#000',shadowOpacity:.15,shadowRadius:8,shadowOffset:{width:0,height:3},elevation:3},icon:{fontSize:20,fontWeight:'900'}});
