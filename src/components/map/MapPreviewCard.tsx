import React from 'react';import {StyleSheet,Text,View} from 'react-native';import {Button,Card} from '@/components/ui/Primitives';import {CATEGORIES} from '@/constants/categories';import {LISTING_TYPES} from '@/constants/listingTypes';import {Listing} from '@/types/models';import {useI18n}from'@/lib/i18n';import {formatDistance}from'@/lib/distance';
export function MapPreviewCard({listing,onView}:{listing:Listing;onView:()=>void}){const{t,isRTL}=useI18n();return <View style={s.wrap}><Card><Text style={[s.kicker,{textAlign:isRTL?'right':'left'}]}>{t(LISTING_TYPES[listing.type].labelKey)} · {t(CATEGORIES[listing.category].labelKey)} · {formatDistance(listing.distance_km,t)}</Text><Text style={[s.title,{textAlign:isRTL?'right':'left'}]}>{listing.title}</Text><Button title={t('viewDetails')} onPress={onView}/></Card></View>}
const s=StyleSheet.create({wrap:{position:'absolute',left:16,right:16,bottom:24},kicker:{color:'#6B776F',fontWeight:'700'},title:{fontSize:20,fontWeight:'800',marginVertical:10}});
