import {router}from'expo-router';import {useState}from'react';import {Text}from'react-native';import {Button,Field,Screen}from'@/components/ui/Primitives';import {useAuth}from'@/hooks/useAuth';
export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState('');const a=useAuth();return <Screen><Text style={{fontSize:32,fontWeight:'900',marginVertical:24}}>Welcome back</Text><Field placeholder="Email" autoCapitalize="none" value={email} onChangeText={setEmail}/><Field placeholder="Password" secureTextEntry value={password} onChangeText={setPassword} style={{marginTop:12}}/><Text style={{color:'crimson'}}>{err}</Text><Button title="Log in" onPress={async()=>{try{await a.signIn(email,password);router.replace('/(tabs)')}catch(e:any){setErr(e.message)}}}/><Button title="Create account" variant="ghost" onPress={()=>router.push('/(auth)/signup')} style={{marginTop:12}}/></Screen>}
