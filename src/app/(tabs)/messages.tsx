import {router}from'expo-router';import {useEffect,useState}from'react';import {FlatList,Text,TouchableOpacity}from'react-native';import {Card,Screen}from'@/components/ui/Primitives';import {fetchConversations}from'@/services/messages';import {Conversation}from'@/types/models';
export default function Messages(){const[data,setData]=useState<Conversation[]>([]);useEffect(()=>{fetchConversations().then(setData as any)},[]);return <Screen><Text style={{fontSize:30,fontWeight:'900',marginBottom:16}}>Messages</Text><FlatList data={data} keyExtractor={i=>i.id} renderItem={({item})=><TouchableOpacity onPress={()=>router.push(`/chat/${item.id}`)}><Card><Text style={{fontWeight:'800',fontSize:17}}>{item.listing?.title||'Conversation'}</Text><Text style={{color:'#6B776F'}}>{item.other_user?.full_name||'Neighbor'} · {item.last_message}</Text></Card></TouchableOpacity>} ListEmptyComponent={<Text>No conversations yet. Message an owner from a listing.</Text>}/></Screen>}
