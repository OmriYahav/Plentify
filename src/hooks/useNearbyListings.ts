import {useCallback,useEffect,useState}from'react';import {fetchNearbyListings}from'@/services/listings';import {Category,Listing,ListingType}from'@/types/models';
export function useNearbyListings(coords:{lat:number;lng:number},radiusKm:number,type?:ListingType|null,category?:Category|null,search?:string){const[data,setData]=useState<Listing[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState<string|null>(null);const refresh=useCallback(async()=>{setLoading(true);setError(null);try{setData(await fetchNearbyListings({lat:coords.lat,lng:coords.lng,radiusKm,type,category,search}))}catch(e:any){setError(e.message)}finally{setLoading(false)}},[coords.lat,coords.lng,radiusKm,type,category,search]);useEffect(()=>{refresh()},[refresh]);return{data,loading,error,refresh};}
