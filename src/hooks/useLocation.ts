import {useCallback,useEffect,useState}from'react';import {LocationCoords,LocationTranslationKey,requestCurrentLocation}from'@/lib/location';import {useI18n}from'@/lib/i18n';
export function useLocation(){const{t}=useI18n();const[coords,setCoords]=useState<LocationCoords|null>(null);const[granted,setGranted]=useState<boolean|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const refresh=useCallback(async()=>{setLoading(true);setError(null);try{const r=await requestCurrentLocation();setCoords(r.coords);setGranted(r.granted);setError(r.errorKey?t(r.errorKey as LocationTranslationKey):null)}catch{setCoords(null);setGranted(false);setError(t('locationCurrentFailed'))}finally{setLoading(false)}},[t]);useEffect(()=>{refresh()},[refresh]);return{coords,granted,loading,error,refresh};}
