import {useCallback,useEffect,useState}from'react';import {LocationCoords,requestCurrentLocation}from'@/lib/location';
export function useLocation(){const[coords,setCoords]=useState<LocationCoords|null>(null);const[granted,setGranted]=useState<boolean|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const refresh=useCallback(async()=>{setLoading(true);setError(null);try{const r=await requestCurrentLocation();setCoords(r.coords);setGranted(r.granted);setError(r.error??null)}catch{setCoords(null);setGranted(false);setError('We could not get your current location. Please try again.')}finally{setLoading(false)}},[]);useEffect(()=>{refresh()},[refresh]);return{coords,granted,loading,error,refresh};}
